[providers.python]
engine = "python"
version = "3.10"

[phases]
install = "pip install --upgrade pip && pip install -r requirements.txt"

[phases.setup]
run = "pip install --upgrade pip && pip install -r requirements.txt"

[phases.start]
run = "gunicorn --workers 3 --bind 0.0.0.0:8000 Screener_Webapp:app"